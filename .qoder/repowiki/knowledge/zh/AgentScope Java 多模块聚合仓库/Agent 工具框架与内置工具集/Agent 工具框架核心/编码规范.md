- 工具类通过 `@Tool` 注解声明元数据，由 `MetaToolFactory` 反射解析并注册到 `ToolRegistry`，禁止手动 new 实例绕过注册流程
- 领域工具包（file/coding/mcp/subagent/builtin）只依赖 core 接口，不得反向引用具体实现类
- 系统级能力（文件系统、Shell 命令）必须经 `ToolValidator` 或专用 Validator（如 `UnixCommandValidator`/`WindowsCommandValidator`）放行后方可执行