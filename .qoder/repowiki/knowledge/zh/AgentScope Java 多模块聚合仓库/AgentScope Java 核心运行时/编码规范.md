- 对外扩展点一律采用接口 + SPI（如 `ModelProvider`、`ToolSchemaProvider`、`FileSystemProvider`）+ 注册中心（`ModelRegistry`、`SkillRegistry`、`ToolRegistry`）的发现模式
- 异步流式输出统一基于 Reactor `Flux`，并通过 `StreamingHook` / `AgentEventEmitter` 暴露细粒度事件
- JSON 序列化统一走 `util.JacksonJsonCodec`，禁止直接调用 Jackson API