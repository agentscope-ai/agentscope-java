- 中间件类统一以 `*Middleware` 命名并按固定顺序注册到 HarnessAgent 构建管线中，新增横切能力需遵循该顺序插入
- 可插拔后端（文件系统、存储、沙箱）均通过 `*Spec` + 工厂模式解耦实现，避免直接耦合具体后端
- 跨组件共享的隔离语义集中在 `IsolationScope` 与 `SandboxContext` 两个上下文中传递，中间件通过它们感知当前隔离边界
- 可选功能通过布尔开关（如 `disableMemoryHooks`、`disableSubagents`、`disableToolsConfig`）控制中间件与工具的注册，保持向后兼容