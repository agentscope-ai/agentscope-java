- 所有子模块遵循 io.agentscope.* 包命名规范，core 定义接口，extensions 通过 SPI 实现并在 META-INF/services 注册
- 父 POM 通过 dependencyManagement 集中声明版本，子模块仅引入 artifactId 不写 version
- 扩展模块按能力域分组（model/memory/storage/sandbox/channel/rag/protocol/spring-boot-starter），每个领域一个聚合子 POM