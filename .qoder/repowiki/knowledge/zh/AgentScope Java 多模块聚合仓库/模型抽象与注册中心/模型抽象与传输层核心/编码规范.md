- 所有可插拔组件（Model、HttpTransport、WebSocketTransport）均以接口定义 + 多实现（JDK/OkHttp）模式暴露，通过工厂或 SPI 选择具体实现
- 异步响应一律使用 Reactor 类型（Flux/Mono），禁止在接口层混用回调或阻塞 API
- 异常体系集中在 `ModelException` 及其子类（`ModelHttpException`、`HttpTransportException`、`WebSocketTransportException`），按层次向上抛出