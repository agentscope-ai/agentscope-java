Maven + flatten-maven-plugin (bom 模式) 生成可发布的 BOM；maven-shade-plugin 构建全量 fat jar；spotless+google-java-format(AOSP) 做编译期格式检查；release profile 启用 maven-gpg-plugin 签名与 sonatype-central-publishing-maven-plugin 发布。