- 所有子模块遵循统一的 package 前缀 io.agentscope.*，core 使用 io.agentscope.core，扩展使用 io.agentscope.extensions.*，示例使用 io.agentscope.examples.*
- 扩展模块通过 META-INF/services 或 Spring Boot AutoConfiguration 向 core 暴露实现，禁止在 core 中硬编码具体提供商类