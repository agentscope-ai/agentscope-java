基于 Reactor `Mono<String>` 异步返回系统提示；使用 Java NIO `Files.walk` 与 `MessageDigest.SHA-256` 做内容指纹；zip 包解析走 `java.util.zip.ZipInputStream`，二进制资源统一以 `base64:` 前缀编码；YAML frontmatter 解析采用 SnakeYAML SafeConstructor 并内置简单 key-value 回退解析器。