- 所有可序列化的配置对象（Credential、HttpTransportConfig、WebSocketTransportConfig）均继承自 `CredentialBase`，以统一的 JSON 反序列化入口对外暴露
- 传输层采用工厂 + SPI 模式：`HttpTransportFactory` / `WebSocketTransport` 配合 JDK 与 OkHttp 两套实现，通过配置选择具体实现