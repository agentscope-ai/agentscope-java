{{- if .Values.services.businessSubAgent.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-sub-agent
  namespace: {{ .Values.global.namespace }}
  labels:
    app: business-sub-agent
spec:
  replicas: 1
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
  selector:
    matchLabels:
      app: business-sub-agent
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: business-sub-agent
    spec:
      initContainers:
        {{- include "agentscope.waitForNacos" . | nindent 8 }}
      containers:
        - name: business-sub-agent
          image: {{ .Values.image.registry }}/business-sub-agent:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 10006
              protocol: TCP
          env:
            - name: SERVER_PORT
              value: "10006"
            {{- include "agentscope.commonEnv" . | nindent 12 }}
          resources:
            requests:
              cpu: "1"
              memory: 2048Mi
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
      securityContext: {}
{{- end }}
