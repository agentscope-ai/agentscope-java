# agentscope-extensions-kotlin

This is a Kotlin-only module: its API documentation is written as KDoc on the extension
functions and is available in the `-sources` jar. This placeholder exists because Maven
Central requires every published artifact to ship a `-javadoc` jar, and `maven-javadoc-plugin`
produces none for a module without Java sources.
